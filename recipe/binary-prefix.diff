--- src/ppl-config.cc.in        2022-03-17 01:54:24.000000000 -0700
+++ src/ppl-config.cc.in        2022-03-17 02:00:21.000000000 -0700
@@ -676,7 +676,8 @@
   if (required_ldflags) {
     std::string s = "@libdir@";
     replace_prefixes(s);
-    const std::string ldflags = "@LDFLAGS@";
+    std::string ldflags = "@LDFLAGS@";
+    replace_prefixes(ldflags);
     // `/usr/lib' is the standard library directory:
     // thus it needs not be specified.
     if (s == "/usr/lib")
